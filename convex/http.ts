import { httpRouter } from "convex/server";

const http = httpRouter();

// No HTTP routes active. Stripe webhook has been removed pending
// proper signature verification implementation.

export default http;
